# DEPLOYMENT_CHECKLIST.md

# massage-next 部署联调清单

> 目标：把 `massage-next` 从“本地可开发”推进到“测试环境可部署、生产环境可上线”。
> 使用方式：按顺序逐项勾选；每完成一轮部署联调，就把结果回写到本文件或 `DEVELOPMENT_LOG.md`。

---

## 0. 当前阶段判断

当前项目已经具备：
- 双语官网核心页面
- 预约 API
- 中文后台登录保护
- 预约管理 / 服务管理 / 内容管理
- Gallery / Hero 上传 MVP
- Docker / Nginx / Prisma / MySQL 基础配置

当前还不应直接裸上生产，必须先完成：
- 环境变量收口
- 本地 Docker 联调
- 上传目录持久化验证
- 数据库迁移 / seed 策略验证
- 认证 / 上传 / 健康检查收口

---

## 1. 部署前代码冻结检查

### 1.1 仓库状态
- [ ] 当前分支代码已提交干净（`git status` 无脏变更）
- [ ] 已推送到远端 GitHub
- [ ] 当前部署基线 commit 已记录
- [ ] `README_CN.md` / `ARCHITECTURE.md` / `ROADMAP.md` / `DEVELOPMENT_LOG.md` 已与现状同步

### 1.2 本地构建
- [ ] `npm install` 正常
- [ ] `npx prisma generate` 正常
- [ ] `npm run build` 正常
- [ ] 本地 `npm run dev` 正常
- [ ] `/api/healthz` 可访问

---

## 2. 环境变量收口

> 原则：先把环境变量定义清楚，再部署。不要在服务器上临时猜。

### 2.1 必需变量确认
- [ ] `DATABASE_URL`
- [ ] `ADMIN_EMAIL`
- [ ] `ADMIN_PASSWORD`
- [ ] `ADMIN_NAME`
- [ ] `SESSION_SECRET`（或当前 auth/session 所需密钥）
- [ ] `NEXT_PUBLIC_SITE_URL` / 站点基址（如果代码中有用）
- [ ] SMTP 相关变量（如启用邮件）
- [ ] MySQL root / app 用户密码

### 2.2 文档与模板
- [ ] `.env.example` 已包含全部必需变量
- [ ] 每个变量用途已经在 README 或部署文档解释清楚
- [ ] 本地开发默认值与生产值职责区分清楚
- [ ] 确认 `.env*` 未进入 git

### 2.3 生产安全
- [ ] 生产 `ADMIN_PASSWORD` 已改为强密码
- [ ] `SESSION_SECRET` 已使用随机高强度值
- [ ] 数据库密码不复用默认值
- [ ] SMTP 使用应用专用密码 / 独立凭据

---

## 3. 数据库 / Prisma 检查

### 3.1 Schema 与迁移策略
- [ ] 明确当前采用 `prisma db push` 还是 `prisma migrate`
- [ ] 确认生产环境不依赖临时手工改表
- [ ] `schema.prisma` 与当前代码一致
- [ ] Prisma Client 可在部署环境正常生成

### 3.2 Seed 策略
- [ ] `npm run db:seed` 可执行
- [ ] seed 至少可初始化管理员、服务、FAQ、营业时间、联系方式、Hero、testimonials
- [ ] seed 重复执行不会制造严重脏数据
- [ ] 生产环境是否运行 seed 已有明确策略

### 3.3 数据验证
- [ ] 预约写入数据库正常
- [ ] 服务管理 CRUD 正常
- [ ] 内容管理更新能正确落库
- [ ] 上传后 `File + GalleryImage` 记录正确创建

---

## 4. 本地业务联调

### 4.1 前台检查
- [ ] `/de` 正常
- [ ] `/en` 正常
- [ ] `/services` 正常
- [ ] `/booking` 正常
- [ ] `/about` 正常
- [ ] `/contact` 正常
- [ ] `/gallery` 正常
- [ ] 多语言切换正常
- [ ] `robots.txt` 正常
- [ ] `sitemap.xml` 正常

### 4.2 预约闭环
- [ ] 前台提交预约成功
- [ ] 数据库中生成预约记录
- [ ] 后台预约列表可见
- [ ] 后台预约详情可见
- [ ] 状态修改成功
- [ ] 内部备注保存成功
- [ ] 快捷操作（确认/完成/取消/爽约）正常

### 4.3 服务管理
- [ ] 新建服务正常
- [ ] 编辑服务正常
- [ ] 上下架正常
- [ ] 精选/排序正常
- [ ] 关联预约时删除保护正常

### 4.4 内容管理
- [ ] Hero 文案更新正常
- [ ] FAQ 增删改正常
- [ ] 联系方式更新正常
- [ ] 营业时间更新正常
- [ ] Gallery 条目增删改正常

---

## 5. 上传链专项检查

> 这是部署前最高优先级检查项之一。

### 5.1 上传能力
- [ ] Gallery 图片上传成功
- [ ] Hero 图片上传成功
- [ ] 文件落盘路径正确
- [ ] 数据库记录正确
- [ ] 上传后前台/后台可见

### 5.2 上传安全与边界
- [ ] 文件类型白名单已限制（jpg/jpeg/png/webp 等）
- [ ] 文件大小限制已实现
- [ ] 文件尺寸下限已实现并验证
- [ ] 非图片文件会被拒绝
- [ ] 上传目录不存在时可自动创建
- [ ] 文件名冲突不会覆盖旧文件
- [ ] 错误提示明确

### 5.3 删除 / 替换策略
- [ ] 删除 Gallery 条目时数据库记录删除正常
- [ ] 删除 Gallery 条目时本地文件清理正常
- [ ] Hero 替换后的旧文件策略已明确
- [ ] Gallery 封面唯一性策略已验证
- [ ] 不会误删非本项目文件

### 5.4 持久化策略
- [ ] `public/uploads` 在 Docker 中已挂载卷
- [ ] 容器重建后上传文件仍然存在
- [ ] 文件 URL 在 Nginx / Web 层可稳定访问

---

## 6. 后台认证与安全检查

### 6.1 登录与会话
- [ ] `/admin/login` 正常
- [ ] 未登录访问后台会被拦截
- [ ] 登录后 session 生效
- [ ] 退出后 session 正确失效
- [ ] Cookie 已设置为 `httpOnly`
- [ ] 生产环境下 Cookie `secure` 策略正确
- [ ] `sameSite` 策略已确认

### 6.2 安全增强
- [ ] 管理员密码已使用安全存储策略
- [ ] 登录接口限流方案已明确
- [ ] 预约接口限流方案已明确
- [ ] 敏感环境变量未暴露到客户端
- [ ] 管理后台页面无裸露敏感信息

---

## 7. Docker / Nginx 联调

### 7.1 Docker
- [ ] `docker compose up -d --build` 正常
- [ ] Web 容器正常启动
- [ ] MySQL 容器正常启动
- [ ] Nginx 容器正常启动
- [ ] 容器日志无明显启动错误
- [ ] Prisma Client / 数据库连接在容器内正常

### 7.2 Nginx
- [ ] 反向代理路径正确
- [ ] 静态资源可访问
- [ ] 上传图片可访问
- [ ] 健康检查可访问
- [ ] 端口映射与域名规划清楚

### 7.3 持久化
- [ ] MySQL 数据卷已配置
- [ ] uploads 数据卷已配置
- [ ] 重启容器后数据仍在

---

## 8. 测试环境部署清单

### 8.1 服务器准备
- [ ] 服务器可 SSH 登录
- [ ] Docker 已安装
- [ ] Docker Compose 已可用
- [ ] Git 已安装
- [ ] 防火墙规则已规划

### 8.2 代码部署
- [ ] 已拉取正确仓库与分支
- [ ] `.env` 已按测试环境配置
- [ ] 数据库初始化策略已执行
- [ ] 容器已启动
- [ ] 域名 / IP 可访问

### 8.3 测试环境 smoke test
- [ ] 前台首页可访问
- [ ] 前台预约提交成功
- [ ] 后台登录成功
- [ ] 后台可看到预约
- [ ] 图片上传成功
- [ ] 上传文件可访问
- [ ] `/api/healthz` 正常
- [ ] 重启后数据与文件仍保留

---

## 9. 生产环境上线前检查

### 9.1 必做项
- [ ] 正式域名已配置
- [ ] HTTPS / SSL 已配置
- [ ] 生产 `.env` 已确认
- [ ] 生产管理员密码已重置
- [ ] 生产数据库备份策略已存在
- [ ] SMTP 已联调
- [ ] 错误日志可查看

### 9.2 内容检查
- [ ] 联系方式为真实信息
- [ ] 地址为真实信息
- [ ] Hero / Gallery 使用正式图片
- [ ] FAQ 为正式内容
- [ ] 文案无占位内容

### 9.3 SEO / 展示
- [ ] title / description 已检查
- [ ] `robots.txt` 已检查
- [ ] `sitemap.xml` 已检查
- [ ] 品牌名、电话、地址一致
- [ ] 本地 SEO 信息完整

---

## 10. 上线后第一轮观察

### 上线后 24 小时内确认
- [ ] 主页访问正常
- [ ] 预约 API 正常
- [ ] 后台登录正常
- [ ] 图片访问正常
- [ ] 容器无反复重启
- [ ] 数据库连接稳定
- [ ] 错误日志无明显异常

---

## 11. 当前建议执行顺序

### 现在立刻做
1. [ ] 收口 `.env.example`
2. [ ] 本地完整跑一遍预约/后台/上传链
3. [ ] 明确 Prisma 迁移与 seed 策略
4. [ ] 本地 `docker compose up --build` 联调
5. [ ] 给 uploads 配持久化卷

### 本周内做
6. [ ] 补上传校验与 Hero 旧文件回收策略
7. [ ] 补登录/预约限流方案
8. [ ] 准备测试环境 `.env`
9. [ ] 上测试环境做 smoke test
10. [ ] 再决定生产上线窗口

---

## 12. 部署结果记录模板

### 本次部署信息
- 日期：2026-03-13
- 环境：本地（非 Docker，生产模式启动测试）
- 分支：main
- Commit：fed389a
- 操作人：OpenClaw

### 结果
- 成功项：
  - `npm run build` 成功
  - 本地生产模式可启动
  - `/de` 返回 200
  - `/admin/login` 返回 200
  - `/robots.txt` 返回 200
  - `docker compose up -d --build` 成功
  - Docker 下 `/` 返回 200
  - Docker 下 `/admin/login` 返回 200
  - Docker 下 `/api/healthz` 返回 `{"status":"ok"}`
  - Docker standalone 启动方式已验证通过
  - `.dockerignore` 优化后 build context 已显著下降
- 失败项：
  - 非 Docker 本地生产模式测试时，数据库依赖曾未接通
- 发现问题：
  - Prisma 在构建阶段仍会提示 OpenSSL / libssl 兼容警告，但当前镜像已可完成构建并正常运行
- 回滚措施：
  - Docker 服务目前保持运行，可按需执行 `docker compose down`
- 下一步：
  - 在 Docker 环境内补数据库迁移 / seed 验证
  - 继续做后台业务链 smoke test（登录、预约、内容、上传）
